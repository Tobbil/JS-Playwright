name: Playwright Tests
on:
  push: 
    branches: [ main ]
jobs:
  playwright-tests:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v2
      - name: Say hello
        run: echo Hello!
